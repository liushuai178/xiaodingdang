<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xdd.mapper.UserMapper">
    <resultMap id="UserMap" type="com.xld.pojo.User">
        <id column="id" property="id"></id>
        <result column="avatar" property="avatar"></result>
        <result column="nickname" property="nickname"></result>
        <result column="sex" property="sex"></result>
        <result column="birthday" property="birthday"></result>
        <result column="tel" property="tel"></result>
    </resultMap>

    <select id="getMyInfo" resultType="com.xld.pojo.User" parameterType="int">
        select id, avatar, nickname, sex, birthday, tel from user where id = #{id}
    </select>

    <update id="updateAll" parameterMap="UserMap">
        update user set avatar = #{avatar}, nickname = #{nickname}, sex = #{sex}, birthday = #{birthday}, tel = #{tel} where id = #{id}
    </update>

    <update id="updateAvatar">
        update user set avatar = #{avatar} where id = #{id}
    </update>

    <update id="updateNickname">
        update user set nickname = #{nickname} where id = #{id}
    </update>

    <update id="updateSex">
        update user set sex = #{sex} where id = #{id}
    </update>

    <update id="updateBirthday" parameterType="date">
        update user set birthday = #{birthday} where id = #{id}
    </update>

</mapper>